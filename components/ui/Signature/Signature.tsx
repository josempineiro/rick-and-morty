import React from "react";
import styles from "./Signature.module.css";

type Props = {};

const Signature = (props: Props) => {
  return <div className={styles.signature}>Leman.dev</div>;
};

export default Signature;
